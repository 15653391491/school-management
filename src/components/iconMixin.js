import {Expand, Fold, Menu, RefreshLeft, CaretTop, CaretBottom} from '@element-plus/icons-vue'

export default {
    components: {
        Expand,
        Fold,
        Menu,
        RefreshLeft,
        CaretTop,
        CaretBottom
    }
}